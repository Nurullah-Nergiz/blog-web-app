// import { Link as RouterLink } from "react-router-dom";

// export default function Link({ href, className, children }) {
//    return (
//       <RouterLink
//          className={className}
//          to={`${window.location.protocol}//${window.location.hostname}:${window.location.port}${href}`}>
//          {children}
//       </RouterLink>
//    );
// }
